## Lens for this run: operations
You are responsible only for the operations axis: migrations and their rollback, behaviour on
dependency failure and timeout, idempotency of retries, retries and backoff, health checks and
environment configuration, logs and metrics for diagnosis, resources and leaks, rollout compatibility
(old and new code running side by side). Include findings outside this axis only at critical severity.
