# Вводная для рецензента

<!-- Заполняет оркестратор (тот, кто заказывает ревью). Комментарии удалить.
     Чем точнее вводная, тем меньше рецензент блуждает и тем меньше ложных находок.
     Не вкладывай сюда код и дифф: рецензент прочитает их сам в снапшоте. -->

## Интент
<!-- Что и зачем менялось, 2–5 строк. Какое поведение должно измениться, какое — остаться.
     Ссылка на задачу или спеку, если она лежит в репозитории. -->

## Система и цена ошибки
<!-- Домен, кто пользователи, что происходит при ошибке: деньги, персональные данные,
     простой, юридические последствия. 2–5 строк. -->

## Стек и неочевидные свойства
<!-- Версии и то, что нельзя вывести из кода: «SELECT'ы идут по read-реплике и не видят
     незакоммиченное», «воркеры работают в другой таймзоне», «провайдер ретраит с новым id». -->

## Как запускать
<!-- Точные команды. Что доступно (БД, docker, внешние сервисы), а что нет — скажи прямо. -->
- Все тесты: `...`
- Целевые тесты: `...`
- Статика / типизация / линтер: `...`
- Недоступно в этом окружении: ...

## Данные стенда (только для plan-режима)
<!-- `--mode plan` просит рецензента измерить допущения дизайна на реальных данных. Без этого
     раздела он может только читать документ, а это вкусовщина. Назови стенд или копию — НИКОГДА
     не прод — и точный способ до неё добраться. Для ревью диффа раздел не нужен. -->
- База, на которой можно пробовать (стенд или копия, не прод): ...
- Как подключиться / открыть сессию: `...`
- Запись разрешена только внутри откатываемой транзакции: да / нет
- Самые вырожденные реальные записи, на которые стоит посмотреть (id или запрос, который их находит): ...

## Карта репозитория
<!-- 2–3 строки на директорию: домен, инфраструктура, тесты, документация. Только то,
     что нужно, чтобы не блуждать. -->

## Что не ревьюить
vendor/, node_modules/, сгенерированный код, снапшоты, миграции старше текущей задачи

## Приоритеты этого прогона
<!-- В порядке важности для ЭТОГО проекта, например:
     1. корректность денег и идемпотентность  2. authn/authz  3. миграции и консистентность
     4. эксплуатация: отказ зависимостей, логи  5. пробелы в тестах -->
1.
2.
3.

## Известные решения
<!-- Сознательные компромиссы, известные долги, то, что уже в бэклоге. Рецензент не должен
     считать это находками. Именно этот раздел и «Интент» скрывает флаг --blind. -->
